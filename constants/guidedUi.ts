/** Brief §11h — Guided mode accessibility targets */
export const guidedTabBar = {
  minHeight: 62,
  labelFontSize: 13,
  iconSize: 26,
} as const;

export const guidedScreen = {
  bodyFontSize: 18,
  labelFontSize: 14,
  buttonFontSize: 20,
  minTapHeight: 56,
} as const;
